name: 'Yarn Audit'
description: 'Runs "yarn audit" on NodeJS Project'
inputs:
  severity-level:
    description: 'Min Severity Level to Report'
    required: true
runs:
  using: 'node16'
  main: 'dist/index.js'